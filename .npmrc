; The docusaurus-theme-search-typesense plugin declares a conservative
; @docusaurus/core peer range (<3.9) that lags behind the versions it
; actually works with. Allow installs to proceed past that mismatch.
legacy-peer-deps=true
